name: Schedule Worker

on:
  schedule:
  - cron: '*/5 * * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        python-version: '3.10'


    - run: ls -al
    - run: python autoPy.py
    - run: ls -al
    

